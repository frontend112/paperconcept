// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
  relationMode = "prisma"
}

// model Store {
//   id String @id @default(uuid())
//   name String @unique
//   userId String @unique
//   // billboards Billboard @relation(fields: [name], references: [id])
// }

// model Billboard {
//   id String @id @default(uuid())
//   billboard Store @relation(fields: [title], references: [name])
//   title String @unique
// }

// we need category, product, order, orderitem, size, color, image
// ? store billboard
